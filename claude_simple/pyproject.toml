[project]
name = "claude-simple"
version = "0.1.0"
description = "Minimal CLI that sends a prompt to Claude."
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "click>=8.1.0",
    "pyyaml>=6.0",
]

[project.scripts]
claude-simple = "main:cli"
